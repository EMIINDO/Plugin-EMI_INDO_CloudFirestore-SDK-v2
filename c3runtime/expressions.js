"use strict";
{
    globalThis.C3.Plugins.EMI_INDO_CloudFirestore.Exps = {
        Data()
        {
            return (JSON.stringify(this.data));
        },

        ErrorMsg()
        {
            return this.errorMsg;
        },

        EventResponse()
        {
            return this.eventResponse;
        }
    };
}